apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs
provisioner: moresophy/nfs-provisioner # must match PROVISIONER_NAME in deployment env
parameters:
  archiveOnDelete: "false"
mountOptions:
  - soft
  - timeo=30
  - retrans=3
