{{- if .Values.rbac.create }}
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  labels:
    {{- include "nfs-provisioner.labels" . | nindent 4 }}
  name: run-{{ template "nfs-provisioner.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ template "nfs-provisioner.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: {{ template "nfs-provisioner.fullname" . }}-runner
  apiGroup: rbac.authorization.k8s.io
{{- end }}
