{{- if .Values.rbac.create }}
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  labels:
    {{- include "nfs-provisioner.labels" . | nindent 4 }}
  name: leader-locking-{{ template "nfs-provisioner.fullname" . }}
  namespace: {{ .Release.Namespace }}
subjects:
  - kind: ServiceAccount
    name: {{ template "nfs-provisioner.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: leader-locking-{{ template "nfs-provisioner.fullname" . }}
  apiGroup: rbac.authorization.k8s.io
{{- end }}
